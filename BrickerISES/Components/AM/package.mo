within BrickerISES.Components;
package AM "Ambient Conditions Package, customized for ThermoCycle Library. Developed at CENER"
    import SI = Modelica.SIunits;
    // The model allows to take the ambient condition from a table implemented in Modelica, from a text file, or from an external file performing the interpolation through an external C
    // function. The only option supported by this version is the one based on tables directly implemented in Modelica.

    //encapsulated

end AM;
